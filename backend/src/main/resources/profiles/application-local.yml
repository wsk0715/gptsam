server:
  port: ${SERVER_PORT_LOCAL}
  base-url: ${SERVER_BASE_URL_LOCAL}
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL_LOCAL}
    username: ${DB_USERNAME_LOCAL}
    password: ${DB_PASSWORD_LOCAL}
